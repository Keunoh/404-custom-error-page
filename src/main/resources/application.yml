server:
  port: 8989

  error:
    whitelabel:
      enabled: false

    path: /error

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.web.ErrorMvcAutoConfiguration

  output:
    ansi:
      enabled: always